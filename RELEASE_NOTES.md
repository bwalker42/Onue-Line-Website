# v2.1.0-alpha.1 — Foundation

The first persistent Onue Line Cruises codebase. It replaces earlier single-page prototypes with distinct routes, shared styling, structured data, and release documentation.
