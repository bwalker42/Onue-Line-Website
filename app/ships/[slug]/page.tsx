import{notFound}from"next/navigation";import Link from"next/link";import{ships}from"../../data";export function generateStaticParams(){return ships.map(s=>({slug:s.slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const s=ships.find(x=>x.slug===slug);if(!s)notFound();return <><section className="pageHero"><p className="eyebrow">{s.className}</p><h1>{s.name}</h1><p>{s.tagline}</p></section><section className="content"><div className="specs"><div><strong>{s.capacity}</strong>Guest capacity</div><div><strong>{s.cabins}</strong>Cabins</div><div><strong>{s.restaurants}</strong>Restaurants</div></div><div className="grid"><article className="card"><h2>Life aboard</h2><p>Light-filled rooms, open decks, and gracious service.</p></article><article className="card"><h2>Cabins</h2><p>Interior $50, Oceanview $250, Suite $500 per guest.</p></article><article className="card"><h2>Deck plans</h2><p>Detailed maps arrive in a future milestone.</p></article></div><Link className="btn" href="/booking">Book {s.name}</Link></section></>}
