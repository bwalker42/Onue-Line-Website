import{notFound}from"next/navigation";import Link from"next/link";import{destinations}from"../destinationData";export function generateStaticParams(){return destinations.map(d=>({slug:d.slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const d=destinations.find(x=>x.slug===slug);if(!d)notFound();return <><section className={`destinationHero ${d.slug}`}><p className="eyebrow">ONUE LINE DESTINATIONS</p><h1>{d.name}</h1><p>{d.intro}</p><div><span>{d.season}</span><span>{d.days}</span></div></section><section className="content destinationDetail"><div><p className="eyebrow dark">SIGNATURE PORTS</p><h2>Arrive somewhere remarkable.</h2><div className="portTimeline">{d.ports.map((p,i)=><article key={p}><span>{i+1}</span><div><small>PORT {String(i+1).padStart(2,"0")}</small><h3>{p}</h3><p>{i===0?"Embarkation and a warm Onue Line welcome.":i===d.ports.length-1?"A memorable final call before the journey home.":"A full day to explore with curated shore experiences."}</p></div></article>)}</div></div><aside><p className="eyebrow dark">VOYAGE HIGHLIGHTS</p>{d.highlights.map(h=><div className="highlight" key={h}><span>◇</span><h3>{h}</h3><p>Available as a curated shore experience on selected sailings.</p></div>)}<Link className="btn" href="/booking">Find a {d.name} voyage</Link></aside></section></>}
