import Link from"next/link";import{PageHero}from"../components/PageHero";import{destinations}from"./destinationData";export default function Page(){return <><PageHero kicker="DESTINATION COLLECTION" title="The world, by water.">Unpack once and discover coastlines, cultures, and remarkable ports through thoughtfully paced Onue Line voyages.</PageHero><section className="content destinationIntro"><div><p className="eyebrow dark">FIND YOUR HORIZON</p><h2>Eight regions. Countless ways to wonder.</h2></div><p>Every itinerary balances landmark ports with time to breathe. Explore sailing seasons, signature calls, and the experiences that define each region.</p></section><section className="destinationGrid">{destinations.map((d,i)=><Link className={`destinationCard destination${i}`} href={`/destinations/${d.slug}`} key={d.slug}><small>{d.season} · {d.days}</small><h2>{d.name}</h2><p>{d.intro}</p><div>{d.ports.slice(0,3).map(p=><span key={p}>{p}</span>)}</div><b>Explore destination →</b></Link>)}</section></>}
