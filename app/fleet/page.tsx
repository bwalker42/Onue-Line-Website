import Link from"next/link";import{ships}from"../data";import{PageHero}from"../components/PageHero";export default function Page(){return <><PageHero kicker="NINE SHIPS · ONE STANDARD" title="Meet the Onue fleet.">Each ship has its own character, united by gracious service and the unmistakable spirit of Onue Line.</PageHero><section className="content"><div className="grid">{ships.map(s=><Link className="card" href={`/ships/${s.slug}`} key={s.slug}><small>{s.className}</small><h2>{s.name}</h2><p>{s.tagline}</p><div className="specs"><div><strong>{s.capacity}</strong>guests</div><div><strong>{s.cabins}</strong>cabins</div><div><strong>{s.restaurants}</strong>dining</div></div><b>Explore ship →</b></Link>)}</div></section></>}
