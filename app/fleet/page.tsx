import Link from"next/link";import{ships}from"../data";import{PageHero}from"../components/PageHero";export default function Page(){return <><PageHero kicker="NINE SHIPS · ONE STANDARD" title="Meet the Onue fleet.">Each ship has its own character, united by gracious service and the unmistakable spirit of Onue Line.</PageHero><section className="content"><div className="fleetGrid">{ships.map((s,i)=><Link className="fleetTile" href={`/ships/${s.slug}`} key={s.slug}><img src={i===0?"/images/fleet/goldentanic.png":i===7?"/images/fleet/bulltannic-3-hero.png":"/images/fleet/sister-ships.png"} alt={`${s.name} at sea`}/><div><small>{s.className}</small><h2>{s.name}</h2><p>{s.tagline}</p><b>{s.capacity.toLocaleString()} guests · {s.cabins.toLocaleString()} cabins · {s.restaurants} restaurants</b></div></Link>)}</div></section></>}
