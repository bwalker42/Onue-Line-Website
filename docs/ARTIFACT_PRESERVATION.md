# Artifact preservation policy

The editable source directory and every versioned `.tgz` release are retained project artifacts. They must not be deleted, replaced, renamed, moved, or cleaned up unless the user explicitly requests that exact operation.

New versions are created alongside previous versions. Workspace refreshes may remove mirrored files; that is an environment persistence issue and is not authorization to overwrite or discard an authoritative local copy.
